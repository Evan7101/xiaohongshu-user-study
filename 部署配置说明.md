# 部署配置说明

## 飞书应用配置（已完成）

以下是你的飞书应用配置信息，**请勿将此文件内容提交到 GitHub**。

| 环境变量名 | 值 | 说明 |
|:---|:---|:---|
| `FEISHU_APP_ID` | `cli_a975d7c0ea781bb3` | 飞书企业自建应用 ID |
| `FEISHU_APP_SECRET` | `Su4dhhmiMbIg6T0aoWjZiftDgqNgW1qF` | 飞书应用密钥 |
| `FEISHU_APP_TOKEN` | `RQErbrmbFarvAKsF0SHcfySUnKh` | 多维表格 App Token |
| `FEISHU_TABLE_ID` | `tblnSLp6LpbVrUM9` | 数据表 Table ID |

> ⚠️ **安全提醒**：App Secret 是敏感信息，不要截图发到公开群，不要写进代码里提交到 GitHub。

---

## 部署到 Vercel 的完整步骤

### 第 1 步：准备 GitHub 仓库

1. 在 [github.com](https://github.com) 创建一个新仓库（如 `xiaohongshu-user-study`）
2. 把本地项目推送到仓库：

```bash
cd C:\code_project\xiaohongshu-user-study
git init
git add .
git commit -m "init: user study web app"
git branch -M main
git remote add origin https://github.com/你的用户名/xiaohongshu-user-study.git
git push -u origin main
```

> 注意：`.gitignore` 已配置好，`.env.local` 不会被提交。

---

### 第 2 步：在 Vercel 导入项目

1. 打开 [vercel.com](https://vercel.com)，用 GitHub 账号登录
2. 点击 **「Add New...」** → **「Project」**
3. 找到 `xiaohongshu-user-study` 仓库，点击 **「Import」**
4. Framework Preset 选择 **「Other」**（因为我们是纯静态 + Edge Function）
5. 点击 **「Deploy」**

Vercel 会自动识别 `vercel.json` 中的 Edge Function 配置并部署。

---

### 第 3 步：配置环境变量（关键）

部署完成后（或部署前），进入项目设置：

1. Vercel Dashboard → 你的项目 → **「Settings」** → **「Environment Variables」**
2. 逐个添加以下 4 个环境变量：

| Name | Value |
|:---|:---|
| `FEISHU_APP_ID` | `cli_a975d7c0ea781bb3` |
| `FEISHU_APP_SECRET` | `Su4dhhmiMbIg6T0aoWjZiftDgqNgW1qF` |
| `FEISHU_APP_TOKEN` | `RQErbrmbFarvAKsF0SHcfySUnKh` |
| `FEISHU_TABLE_ID` | `tblnSLp6LpbVrUM9` |

3. 添加完后，点击 **「Save」**
4. 如果已经部署过，需要 **重新部署** 一次环境变量才生效：
   - 进入 **「Deployments」** 标签
   - 找到最新一次部署，点击右侧的 **「⋮」** → **「Redeploy」**

---

### 第 4 步：验证飞书应用是否已发布

如果提交数据时飞书返回 **权限错误**（如 `"code": 11271 或 99991672"`），请检查：

1. 飞书开放平台 → 你的应用 → **「版本管理与发布」**
2. 确认应用状态是 **「已发布」**（或至少「已上线」）
3. 如果不是，点击 **「创建版本」** → 填写版本号（如 1.0.0）和更新说明 → **「保存并发布」**

> 只有发布后的应用才能通过 API 调用。

---

### 第 5 步：测试提交

1. 用浏览器打开 Vercel 分配的域名（如 `https://xiaohongshu-user-study-xxx.vercel.app`）
2. 完整填写一遍问卷
3. 提交后检查飞书多维表格，确认数据已写入

---

## 常见问题

**Q: 提交时提示 "Missing Feishu environment variables"**
> A: 环境变量没有正确配置，或重新部署后未生效。请重新检查第 3 步。

**Q: 提交时提示 "Feishu auth error" 或 "No permission"**
> A: 应用未发布，或权限未正确开通。请检查第 4 步。

**Q: 提交时提示 "Feishu create record error"**
> A: 多维表格的字段名和代码中的不一致。请检查表格中是否有所有必需的列（如 `提交时间`、`受访者ID`、`用户1_A1_整体自然度` 等）。

**Q: 图片加载很慢**
> A: 把图片压缩到单张 500KB 以内，或上传到国内图床（如腾讯云 COS、阿里云 OSS）。

---

## 自定义域名（可选）

如果嫌 `vercel.app` 域名太长，可以绑定自定义域名：

1. Vercel Dashboard → 你的项目 → **「Settings」** → **「Domains」**
2. 输入你的域名（如 `study.yourdomain.com`）
3. 按提示在域名服务商处添加 DNS 记录
4. 等待 DNS 生效（通常几分钟到几小时）
