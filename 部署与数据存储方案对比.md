# 部署与数据存储方案对比

## 前提条件

- 预估文件：130张图片（5用户×10帖×2张 + Section B 30张）+ 网页代码
- 预估大小：如果图片每张压缩到 300-500KB，总大小约 **40-65MB**
- 样本量：预估 30-100 人填写
- 受访者：主要在国内，用手机微信/浏览器打开

---

## 方案一：纯前端 + CSV手动回收（最简单，零成本）

| 项目 | 方案 |
|:---|:---|
| **网页部署** | **GitHub Pages**（免费）或 **Vercel**（免费） |
| **图片存储** | 放在项目文件夹里，和网页一起部署 |
| **数据收集** | 前端 `localStorage` 暂存答题进度，结束页**自动生成 CSV 文件供下载** |
| **回收方式** | 受访者填完手动把 CSV 发给你（微信/邮件/问卷星文件题） |
| **优点** | 完全零后端代码；不用担心API限额；数据完全掌控在自己手里；国内可访问（Vercel略好于GitHub Pages） |
| **缺点** | 需要受访者手动操作一步（下载CSV并发送）；可能有少数人忘记发 |
| **适合** | 样本量小（<50人）、想最快上线、不想碰任何后端 |

### 具体操作

1. 我把代码写好，你把项目文件夹推送到 GitHub 仓库
2. 在仓库设置里开启 GitHub Pages，或一键部署到 Vercel
3. 得到一个 `https://xxx.vercel.app` 或 `https://xxx.github.io` 链接
4. 把链接发给受访者，他们手机浏览器打开即可填写
5. 结束页自动弹出 CSV 下载，受访者保存后发回给你

---

## 方案二：纯前端 + 免费表单API（推荐，自动回收）

| 项目 | 方案 |
|:---|:---|
| **网页部署** | **Vercel**（免费，国内访问较好） |
| **图片存储** | 放在项目文件夹里一起部署 |
| **数据收集** | 接入 **Formspree** 或 **GetForm** 免费版，前端直接 POST 提交 |
| **回收方式** | 数据自动进入 Formspree 后台，可导出 CSV/Excel |
| **优点** | 受访者无感知，提交即完成；自动汇总数据；部署依然简单 |
| **缺点** | 免费版有月提交限额（Formspree 50次/月，GetForm 100次/月） |
| **适合** | 样本量 30-80 人，希望自动回收，不想自建后端 |

### 免费表单服务对比

| 服务 | 免费限额 | 国内访问 | 是否需要注册 |
|:---|:---|:---|:---|
| **Formspree** | 50 次提交/月 | 一般 | 邮箱注册 |
| **GetForm** | 100 次提交/月 | 一般 | 邮箱注册 |
| **FormSubmit** | 1000 次提交/月（限制多） | 一般 | 无需注册（用邮箱验证） |
| **Supabase** | 500MB 数据库 + 无限读/有限写 | 一般 | 邮箱注册 |

### 具体操作

1. 注册 Formspree / GetForm 账号，创建一个表单，得到一个 endpoint URL（如 `https://formspree.io/f/xnqevjdr`）
2. 我把这个 URL 写进前端代码里，提交时自动 POST 数据
3. 部署到 Vercel
4. 数据实时进入后台，你随时登录查看或导出

---

## 方案三：国内静态托管 + 轻量云函数（最稳定，极小成本）

| 项目 | 方案 |
|:---|:---|
| **网页部署** | **腾讯云 COS 静态网站托管**（约 ¥0-5/月，按流量）或 **阿里云 OSS** |
| **图片存储** | 和网页一起放在 COS/OSS 里 |
| **数据收集** | **腾讯云云函数 SCF** 或 **阿里云函数计算 FC**（每月免费额度 100万次调用） |
| **回收方式** | 数据写入云数据库（免费额度内）或直接推送到你的邮箱/企业微信 |
| **优点** | 国内访问最快最稳定；无表单服务限额；完全自主可控 |
| **缺点** | 需要注册云账号、配置 bucket 和云函数，步骤稍多 |
| **适合** | 样本量大（>100人）、对访问速度要求高、愿意花极小成本 |

### 具体操作

1. 注册腾讯云账号，开通 COS 和 SCF
2. 创建一个存储桶（bucket），开启静态网站托管
3. 上传网页代码和图片到存储桶
4. 写一个 20 行的云函数（Node.js/Python）接收 POST 请求，写入数据库或发邮件
5. 得到一个 `https://yourbucket-xxx.cos-website.ap-guangzhou.myqcloud.com` 链接

---

## 我的推荐

| 你的情况 | 推荐方案 |
|:---|:---|
| **想最快上线，不想折腾任何配置** | **方案一**：Vercel + localStorage + CSV下载 |
| **样本30-80人，想自动回收数据** | **方案二**：Vercel + Formspree/GetForm（推荐） |
| **样本>100人，或师兄要求国内稳定访问** | **方案三**：腾讯云 COS + 云函数 |

**个人最推荐方案二**。因为：
- 30-80人的样本，Formspree 50次/月或 GetForm 100次/月完全够用
- 如果你担心不够，可以注册两个 Formspree 表单轮换使用（前端随机选一个提交）
- 部署到 Vercel 后国内手机浏览器打开没问题
- 受访者体验最流畅：填完直接结束，不需要下载文件发给你

---

## 补充：图片加载优化建议

不管选哪个方案，130张图片同时预载会卡死手机。需要做懒加载（lazy loading）：
- Section A：进入用户主页时只加载该用户的20张图，其他用户的图暂不加载
- Section B：当前展示的图片加载高清版，前后各1张预载，其余不加载
- 建议图片压缩到 **宽1080px，质量80%，单张<500KB**

如果你需要，我可以把图片批量压缩脚本也写好。
